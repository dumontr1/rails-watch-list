<h1><%= @list.name %></h1>

<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li><%= bookmark.movie.title %> | <%= bookmark.comment %></li>
  <% end %>
</ul>

<%= link_to 'Back', lists_path, class: "btn btn-primary" %> |
<%= link_to 'Add a movie', new_list_bookmark_path(@list), class: "btn btn-primary" %>

<h1>New</h1>
<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', list_path(@list), class: "btn btn-primary" %>
